# === Secrets — NIKDY do gitu (v repu jen *.example) ===
.env
.env.*
!.env.example
*.local.json
.dev.vars
secrets.*
*.key
*.pfx
*private*.pem
id_rsa

# === Node / web ===
node_modules/
dist/
build/
.wrangler/

# === .NET ===
bin/
obj/
.vs/
*.user
*.suo

# === Python ===
__pycache__/
.venv/
*.pyc

# === OS / editor / logy ===
.DS_Store
Thumbs.db
*.log

# === Zalohy databaze — osobni udaje nezletilych ===
zaloha*.sql
*.sqlite


# generuje se pred kazdym deployem (scripts/gen-version.mjs)
web/version.json
worker/src/version.ts

# generuje se pred kazdym deployem (scripts/gen-dokumenty.mjs) — je to jen
# prevod .md souboru z repozitare do HTML, zdrojem pravdy zustava Markdown
worker/src/dokumenty.ts

